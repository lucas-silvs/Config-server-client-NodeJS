{
  "name": "cloud-config-client",
  "version": "1.0.0",
  "description": "Teste de aplicação cliente em NodeJS para Spring Cloud Config Server",
  "main": "node-with-config-client.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "config": "node node-with-config-client.js",
    "start": "node client-utilizando-dotenv.js",
    "agoravai": "npm run config && npm run start"
  },
  "author": "lucas.parkl@outlook.com",
  "license": "ISC",
  "dependencies": {
    "cloud-config-client": "^1.6.2",
    "dotenv": "^16.0.2",
    "js-yaml": "^3.14.1",
    "node-yaml": "^4.0.1"
  }
}
